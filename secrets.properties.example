# Copy to secrets.properties and fill in. Never commit the real file.
#
# Baked into the APK as BuildConfig.DEFAULT_API_KEY so a fresh install can talk to a gateway
# without typing a key first. Leave empty to ship a build with nothing pre-filled — the user
# then adds a service under 设置 → 服务.
defaultApiKey=
